environment: dev

namespace: dev

replicaCount: 1

image:
  repository: challenge-devops
  pullPolicy: dev
  tag: dev

service:
  type: LoadBalancer
  port: 80

containerPort: 8000

otel:
  endpoint: "http://otel-collector-opentelemetry-collector.observability.svc.cluster.local:4318"
  serviceName: "challenge-devops-api"
  environment: "dev"

